# setup.yml - Environment configuration for mesh-converted
# Generated by isle scaffold
# Source: /home/dustin/Desktop/IsleMesh/test-mesh-apps/python-app-only/mesh-converted/docker-compose.yml

current-setup:
  env: production

environments:
  production:
    domain: python-api.local
    projects:
      mesh-converted: { path: . }
      python-api: { port: '5000' }

  dev:
    domain: localhost
    expose_ports_on_localhost: true
    projects:
      mesh-converted: { path: . }
      python-api: { port: '5000' }
